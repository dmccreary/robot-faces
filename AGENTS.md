# Workspace Instructions

## Image Generation

- Always use Codex's internal image generator as the first and default method for creating or editing images.
- Do not call external image-generation APIs directly, and do not use project scripts that generate images through external APIs.
- If the internal image generator is unavailable or cannot complete the request, stop and ask the user before using any alternative image-generation method.
